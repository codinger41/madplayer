import TrackPlayer from 'react-native-track-player';
import { ALL_TRACKS } from './actionTypes';

export const getAllTracks = () => async dispatch => {
  // const state = await TrackPlayer.getQueue();
  // console.log(state)
  // dispatch({
  //   type: ALL_TRACKS,
  //   tracks: state
  // });
};
